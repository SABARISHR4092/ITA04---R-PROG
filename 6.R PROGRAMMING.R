#11. R Program to Check Whether a Number is Positive, Negative, or Zero
# Input number
num <- -5

# Check condition
if (num > 0) {
  cat("The number is Positive")
} else if (num < 0) {
  cat("The number is Negative")
} else {
  cat("The number is Zero")
}
